
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: loadtest-privileged
  namespace: loadtest
rules:
- apiGroups:
  - extensions
  resourceNames:
  - privileged
  resources:
  - podsecuritypolicies
  verbs:
  - use
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: loadtest-privileged
  namespace: loadtest
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: loadtest-privileged
subjects:
- kind: ServiceAccount
  name: default
  namespace: loadtest